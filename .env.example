# Copy to .env and fill values
# You can either set ADMIN_PASS (plain password, not recommended) or ADMIN_PASS_HASH (bcrypt hash).
# Recommended: generate a bcrypt hash using the provided script and set ADMIN_PASS_HASH

# Example (leave blank until you set):
ADMIN_PASS_HASH=
# Or a temporary plain password (less secure):
ADMIN_PASS=

# Session secret for express-session
SESSION_SECRET=change_this_session_secret

# MongoDB Atlas connection (required in production)
MONGO_URI=
MONGODB_DB_NAME=spicekart

# Optional: MongoDB app user bootstrap (used by `npm run create-mongo-user`)
# MONGO_ADMIN_URI can be an admin-capable URI; defaults to MONGO_URI
MONGO_ADMIN_URI=
MONGO_APP_USER=
MONGO_APP_PASS=

# Server port
PORT=4000
